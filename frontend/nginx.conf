upstream backend {
    server backend:3000;
}

server {
    listen 80;
    server_name localhost;
    client_max_body_size 10M;

    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "strict-origin-when-cross-origin";

    # API endpoints
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check
    location /health {
        proxy_pass http://backend;
    }

    # Static assets with long cache
    location /static/ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, immutable";
        gzip on;
        gzip_types application/javascript text/css;
    }

    # Known SPA routes — serve frontend index.html
    # These MUST come BEFORE the short code catch-all below
    location ~ ^/(login|register|dashboard|analytics) {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # Short code redirects — proxy to backend
    # Matches single-segment alphanumeric paths like /aB3xK7m
    location ~ ^/[a-zA-Z0-9_-]+$ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA fallback — serve index.html for all other routes
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
